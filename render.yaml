services:
  - type: web
    name: pentestkit
    runtime: python
    buildCommand: pip install -r requirements-minimal.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 10000
